# Grafana Datasource — auto-configures Loki on startup
# No manual setup needed in Grafana UI
apiVersion: 1

datasources:
  - name: Loki
    type: loki
    access: proxy
    url: http://loki:3100
    isDefault: true
    editable: false